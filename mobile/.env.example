# URL du serveur web (dev local ou prod)
EXPO_PUBLIC_API_URL=http://localhost:3000

# Typesense (clé read-only — générer via : bun run search:create-key dans le projet web)
EXPO_PUBLIC_TYPESENSE_SEARCH_API_KEY=local-search-key
EXPO_PUBLIC_TYPESENSE_HOST=localhost
EXPO_PUBLIC_TYPESENSE_PORT=8108
EXPO_PUBLIC_TYPESENSE_PROTOCOL=http
